{
  "name": "@vistaroom/development-orchestrator",
  "version": "0.1.0",
  "private": true,
  "description": "Isolated MVP vertical slice of the VistaRoom Development Orchestrator (Architecture Workflow only). Standalone, removable package — no VistaRoom AI runtime dependency.",
  "type": "module",
  "bin": {
    "orchestrator": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "orchestrator": "tsx src/cli.ts"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "yaml": "^2.5.1"
  },
  "devDependencies": {
    "@types/node": "^20.14.2",
    "tsx": "^4.19.1",
    "typescript": "^5.5.4",
    "vitest": "^2.1.9"
  }
}
